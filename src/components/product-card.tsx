"use client";
import Link from "next/link";
import { Plus } from "lucide-react";
import { Button } from "@/components/ui/button";
import { useCart } from "@/components/cart-context";
import { money, priceFor, type Product } from "@/lib/catalog";
export function ProductCard({ product }: { product: Product }) { const cart = useCart(); return <article className="group"><Link href={`/product/${product.slug}`} className="block overflow-hidden bg-secondary"><img src={product.image} alt={`${product.name} in Veloiz packaging`} loading="lazy" width={1200} height={1504} className="aspect-[4/5] w-full object-cover transition duration-500 group-hover:scale-[1.025]" /></Link><div className="grid grid-cols-[1fr_auto] gap-3 pt-4"><div><p className="text-xs uppercase text-muted-foreground">{product.category}</p><Link href={`/product/${product.slug}`} className="font-display text-2xl">{product.name}</Link><p className="mt-1 font-medium">From {money(priceFor(product, 250))}</p>{product.stock <= 8 && <p className="mt-2 text-xs text-primary">Only {product.stock} packs left</p>}</div><Button size="icon" variant="quiet" aria-label={`Add ${product.name}`} onClick={() => cart.add(product, 250, 1)}><Plus /></Button></div></article> }